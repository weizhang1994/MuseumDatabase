-- Produce a list of when each gallery from all museums will be available

SELECT * FROM GalleryAvailability WHERE "Institution" = ${"Institution" (where)||MCA||String||where nullable unformatted ds=11 dt=STRING}$;


SELECT * FROM GalleryAvailability