-- Produce for the head of the museum a listing of all works (including their identifier, name, and insurance value)
-- that are currently in storage of a specific museum.

SELECT * FROM currentstorage WHERE "Institution" = ${"Institution" (where)||MCA||String||where nullable unformatted ds=11 dt=STRING}$;

SELECT * FROM currentstorage;